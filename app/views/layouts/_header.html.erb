<nav class = "navbar navbar-expand-lg navbar navbar-light" style="background-color: #e3f2fd;">
  <div class="container-fluid">
  
   <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
<div class="collapse navbar-collapse" id="navbarSupportedContent">
   <ul class="navbar-nav me-auto mb-2 mb-lg-0">
    <% if user_signed_in? && current_user.role.code == "employee" %>

    <%=link_to 'sign out',destroy_user_session_path, method: :delete,class: "nav-link"%>

    <%=link_to 'leave', new_user_break_path(current_user.id),class: "nav-link"%> 

    <%=link_to 'Attendance', new_user_emp_attendance_path(current_user.id),class: "nav-link"%>

    <%=link_to 'Salary',  user_salaries_path(current_user.id),class: "nav-link"%>

    <%=link_to 'Edit profile',  edit_user_registration_path(:id),class: "nav-link"%>

    <%=link_to 'Holiday',  holidays_path(:id),class: "nav-link"%>

    <%=link_to 'Project',  user_projects_path(current_user.id),class: "nav-link"%>

    <% elsif user_signed_in? && current_user.role.code == "admin" %> 

    <%= link_to 'Sign Out', destroy_user_session_path, method: :delete , class: "nav-link"%>

    <%=link_to 'leave', user_breaks_path(current_user.id),class: "nav-link"%> 

    <%=link_to 'Attendance', user_emp_attendances_path(current_user.id),class: "nav-link"%>

     <%=link_to 'Salary',  new_user_salary_path(current_user.id),class: "nav-link"%> 
     <%=link_to 'Details_Of_Employies',   user_registration_path(current_user.id),class: "nav-link"%> 
 
    <%=link_to 'Holiday',  holidays_path(:id),class: "nav-link"%>

    <%=link_to 'Project',  new_user_project_path(current_user.id),class: "nav-link"%> 

    <% else %>

    <%=link_to "sign_up",new_user_registration_path,class: "nav-link" %>

    <%=link_to "login",new_user_session_path,class: "nav-link"%>

    <% end  %>
        
        <li class="nav-item">
          <a class="nav-link" href="#"></a>
        </li>
       
      </ul>
    
    </div>
  </div>
</nav>